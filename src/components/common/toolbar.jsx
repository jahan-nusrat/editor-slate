import React from 'react'

const Toolbar = ({...props}) => {
  return (
    <div {...props} style={{position:'relative', padding:'1px 18px 17px', margin:'0 -10px', borderBottom:'2px solid #eee', marginBottom:'20px'}}>
    </div>
  )
}

export default Toolbar
